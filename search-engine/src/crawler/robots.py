"""robots.txt parser — check crawl permissions before fetching — stub."""
# TODO: implement


def is_allowed(url: str, user_agent: str = "*") -> bool:
    """Return True if crawling url is permitted by that host's robots.txt."""
    raise NotImplementedError
