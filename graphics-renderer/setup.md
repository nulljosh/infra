# Setup

## Dependencies
- Rust 1.75+
- No external crates for core (outputs PPM)

## Build & Run
```bash
cargo run > output.ppm
# Open with: open output.ppm (macOS) or feh/eog (Linux)
```

## Test
```bash
cargo test
```
